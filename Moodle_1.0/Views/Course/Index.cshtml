@model IEnumerable<Moodle_1._0.Models.Course>
@{
    ViewBag.Title = "Index";
}

<h2>My Courses</h2>
@foreach(var i in Model)
{
    @Html.ActionLink(i.CourseName, "Details",new { id=i.CourseId });
    <br /><hr />
}

